global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  # Scrape the PC Monitor server
  - job_name: 'pc-monitor'
    static_configs:
      - targets: ['host.docker.internal:8000']
    metrics_path: '/metrics'
